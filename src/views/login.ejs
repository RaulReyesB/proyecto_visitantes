<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= namePage %></title>
  <!-- Agregar los enlaces de Bootstrap CSS -->
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styleLogin.css">
  <link rel="stylesheet" href="/css/footer.css">

</head>
<body>
  <div class="custom-background"></div>
  <div class="container d-flex align-items-center justify-content-center min-vh-100 flex-column flex-lg-row">
    <div class="welcome-container order-2 order-lg-1">
      <h1>¡Bienvenido!</h1>
      <img src="/img/rtyh_logo.webp" alt="Logo" class="logo-img">
    </div>
    <div class="login-container order-1 order-lg-2">
      <h2>Iniciar Sesión</h2>
      <p>Sistema de Registro y Acceso a Radio y Televisión Hidalgo</p>
      <form action="/inicio" method="post">
        <div class="form-group">
          <input type="text" onkeyup="this.value = this.value.toUpperCase();" class="form-control" name="name" value="<%= typeof formData !== 'undefined' && formData.name ? formData.name : '' %>" placeholder="Nombre de usuario" required>
        </div>
        <div class="form-group">
          <input type="password" class="form-control" name="password" placeholder="Contraseña" required>
        </div>
        <button type="submit" class="btn btn-primary btn-block">Acceder</button>
      </form>
      <% if (errors.length > 0) { %>
      <div class="mt-3">
        <ul class="list-group">
          <% errors.forEach(error => { %>
          <li class="list-group-item list-group-item-danger"><%= error.msg %></li>
          <% }) %>
        </ul>
      </div>
      <% } %>
    </div>
  </div>
  <div>
    <footer>   
         <%- include('templates/footer') %>
    </footer>
</div>
  <!-- Agregar los scripts de Bootstrap JS -->
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>
